apiVersion: v1
kind: pod
metadata:
  name: resource
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      requests:
        memory: "64M"
        cpu: "250m"
      limits:
        memory: "128M"
        cpu: "500m"
  